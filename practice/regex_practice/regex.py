import re


print(re.findall(r'\d{1,7}\s\w+\.\s\w+\s\w+', '16551 s. bradley rd'))


# A Python program to generate squares from 1
# to 100 using yield and therefore generator

# An infinite generator function that prints
# next square number. It starts with 1


class KFLAY:
    def __init__(self, cutie_level):
        self.cutie_level = cutie_level
        self.imperfections = []

the_girl = KFLAY(100)

to_sort = [5,4, 1, 2, 3]
print(sorted(to_sort))
